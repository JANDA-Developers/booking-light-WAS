import { JDcard } from '@janda-com/front';
import React, { useState } from 'react';

interface IProp { }

export const CalculatePayment: React.FC<IProp> = () => {
    const [bookMark, setBookMark] = useState<"" | "" | "">("")

    return <JDcard>

    </JDcard>;
};
